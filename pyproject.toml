[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "edn-os"
version = "0.1.0"
description = "EDN OS — operating system software for EDN Systems"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "EDN Systems" }]

dependencies = [
    "msal>=1.33,<2",
    "streamlit>=1.40,<2",
]
[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]

[tool.ruff]
target-version = "py311"
line-length = 88
src = ["src", "tests", "poc"]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "UP",
    "B",
    "N",
    "SIM",
    "RUF",
]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["edn", "poc"]
mypy_path = "src"
explicit_package_bases = true


[[tool.mypy.overrides]]
module = ["streamlit", "streamlit.*"]
follow_imports = "skip"

[[tool.mypy.overrides]]
module = "pypff"
ignore_missing_imports = true

[tool.coverage.run]
source = ["edn"]
branch = true
