"""Platform-wide exception types.

Defines the shared error hierarchy (configuration, validation, storage,
security, external dependency). Domain-specific errors remain in feature
modules such as ``edn.memory``.
"""
